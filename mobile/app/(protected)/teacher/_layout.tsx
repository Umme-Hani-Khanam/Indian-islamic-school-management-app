import { Stack } from 'expo-router';

export default function TeacherLayout() {
  return <Stack screenOptions={{ headerStyle: { backgroundColor: '#0066cc' }, headerTintColor: '#fff', headerTitleStyle: { fontWeight: 'bold' } }} />;
}
